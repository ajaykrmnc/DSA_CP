# Numbers containing 1, 2 and 3

**Problem Statement:**
Given an array of n numbers, print only those numbers whose digits are from the set {1, 2, 3}. For each number in the array, check if all its digits belong to the set {1, 2, 3}. If a number contains any digit other than 1, 2, or 3, it should not be included in the output. You can solve this by converting each number to a string and checking each digit, or by using mathematical operations to extract digits. The solution should have O(n*d) time complexity where d is the average number of digits.

```cpp
Given an array arr[] of n numbers.
The task is to print only those numbers whose digits are from set {1,2,3}.
```

```cpp

```