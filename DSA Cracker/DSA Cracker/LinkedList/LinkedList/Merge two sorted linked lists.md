# Merge two sorted linked lists

**Problem Statement:**
Given two sorted linked lists, merge them into a single sorted linked list. The merged list should be made by splicing
together the nodes of the first two lists while maintaining the sorted order. The function should return the head of the
merged linked list. You can solve this problem either iteratively or recursively. The iterative approach uses O(1) extra
space while the recursive approach uses O(m+n) space due to the call stack, where m and n are the lengths of the two lists.
Both approaches have O(m+n) time complexity for merging the lists.

```cpp

```