# Binary Tree to CDLL

**Problem Statement:**
Given a binary tree, convert it to a Circular Doubly Linked List (CDLL) in place. The left and right pointers in nodes are to be used as previous and next pointers respectively in the converted CDLL. The order of nodes in CDLL should be the same as the inorder traversal of the binary tree. The first node of inorder traversal should be the head of the CDLL. Use inorder traversal and maintain previous node pointer to link nodes in CDLL format, ensuring the last node points back to the first node to make it circular.

```cpp

```