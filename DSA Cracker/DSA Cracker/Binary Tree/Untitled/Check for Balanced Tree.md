# Check for Balanced Tree

**Problem Statement:**
Given a binary tree, determine if it is height-balanced. A height-balanced binary tree is defined as a binary tree in which
the left and right subtrees of every node differ in height by no more than 1. For each node, calculate the height of left
and right subtrees and check if the absolute difference is at most 1. Use a recursive approach that returns both the height
and balance status to avoid redundant calculations. Time complexity is O(n) and space complexity is O(h) where h is height.

```cpp
// Implementation needed
```